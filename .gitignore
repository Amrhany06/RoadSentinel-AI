# Raw/large media datasets -- never commit these, see docs/ for download instructions
data/accident_images/
data/motorcycle_accident_videos/
*.mp4
*.avi
# ...except the small, curated demo clips and their precomputed cache -- these ARE committed
!demo/**
!demo/**/*.mp4
!data/*.csv
data/*_backup.csv


__pycache__/
*.pyc
.env
.streamlit/secrets.toml
dispatch_tickets.db
.venv/
.ipynb_checkpoints/

# Model artifacts: DO commit the small ones produced by this project
# (svm_triage_pipeline.joblib, severity_kmeans.joblib, response_time_regressor.joblib,
# hotspot_kmeans.joblib are all KB-sized) -- only ignore large ones explicitly if you add any.

# Local study guides & defense reference manuals (kept in parent workspace)
*GUIDE*.md
*EXPLANATION*.md
!docs/MASTER_STUDY_GUIDE_EXPERT.md


